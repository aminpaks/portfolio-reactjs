import styled from 'src/Theme';

export const StyledAnimatedIcon = styled.div`
  width: 6em;
  position: relative;

  .icon {
    margin: 0.2em;
    user-select: none;
    pointer-events: none;
  }
`;
